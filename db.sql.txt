CREATE DATABASE banking_system;

USE banking_system;

CREATE TABLE Customers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    current_balance DECIMAL(10, 2)
);

CREATE TABLE Transfers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    sender_id INT,
    receiver_id INT,
    amount DECIMAL(10, 2),
    transfer_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (sender_id) REFERENCES Customers(id),
    FOREIGN KEY (receiver_id) REFERENCES Customers(id)
);

SELECT * FROM Customers;

INSERT INTO Customers (name, email, current_balance) VALUES
('ABHINAY', 'abhinay@example.com', 12000.00),
('RAM', 'ram@example.com', 7500.00),
('SITA', 'sita@example.com', 22500.00),
('KRISHNA', 'krishna@example.com', 22000.00);